\documentclass[tikz, border=5mm]{standalone}
\usepackage[spanish]{babel}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{xcolor}
\usetikzlibrary{shapes.geometric, arrows.meta, positioning}

\begin{document}
\begin{tikzpicture}[
  node distance=1.2cm and 2cm,
  every node/.style={align=center, font=\small},
  io/.style={trapezium, trapezium left angle=70, trapezium right angle=110, draw, fill=blue!10, minimum width=2.2cm},
  proc/.style={rectangle, draw, rounded corners, minimum width=2.8cm, minimum height=0.8cm},
  dec/.style={diamond, draw, aspect=1.8, fill=yellow!15, minimum width=2.8cm},
  term/.style={rectangle, draw, rounded corners, fill=green!15, minimum width=2.2cm},
  arrow/.style={-{Stealth[scale=1.2]}, thick}
]

% Input node
\node[io] (entrada) {Mensaje\\del usuario};

% Tier 0 processing
\node[proc, below=of entrada] (tier0) {Tier 0 \\ Zero-inference\\ \tiny 462 patrones + regex};
\node[dec, below=of tier0] (match) {¿Coincide\\patrón?};

% Tier 1 processing
\node[proc, below right=of match, xshift=2cm] (tier1) {Tier 1 \\ Clasificador 1.5B\\ \tiny prompt cerrado 130+ ej.};
\node[dec, below=of tier1] (delega) {¿DELEGAR?};

% Tier 2 processing
\node[proc, below=of delega] (tier2) {Tier 2 \\ Razonador 7B\\ \tiny carga bajo demanda};
\node[term, below left=of match, xshift=-1.5cm] (ejecutar) {Ejecutar\\comando};

% Main flow arrows
\draw[arrow] (entrada) -- (tier0);
\draw[arrow] (tier0) -- (match);

% Yes branch (Tier 0 match)
\draw[arrow] (match.west) -- ++(-1.5,0) node[midway, above, font=\tiny] {Sí} |- (ejecutar.north);

% No branch (goes to Tier 1)
\draw[arrow] (match.south) -- ++(0,-0.3) -| (tier1.west) node[midway, above, font=\tiny] {No};

% Tier 1 to Delegate decision (Added missing vertical line)
\draw[arrow] (tier1.south) -- (delega.north);

% Circuit breaker check
\node[dec, right=of tier1, xshift=1.5cm, fill=orange!15] (cb) {Circuit\\breaker\\¿abierto?};
\draw[arrow] (tier1.east) -- (cb.west);

% Circuit breaker arrow connecting to the right (east) side of Tier 2
\draw[arrow] (cb.south) |- (tier2.east) node[pos=0.75, above, font=\tiny] {Sí (forzar Tier 2)};

% Tier 1 to Tier 2
\draw[arrow] (delega.south) -- ++(0,-0.3) -| (tier2.west) node[midway, above, font=\tiny] {Sí};

% Do not delegate = respond (Elevated node to prevent overlapping text below)
\node[term, below right=of delega, xshift=1.5cm, yshift=0.7cm] (resp) {Responder\\al usuario};
\draw[arrow] (delega.east) -- ++(1.5,0) node[midway, above, font=\tiny] {No} |- (resp.west);

% Tier 2 response
\draw[arrow] (tier2.south) -- ++(0,-0.5) -| (resp.south);

% LRU Cache
\node[proc, right=of tier0, xshift=2.5cm, fill=gray!10, dashed] (cache) {Caché LRU\\\tiny 200 entradas};
\draw[arrow, dashed, gray] (tier0.east) -- (cache.west);
\draw[arrow, dashed, gray] (cache.south) -- ++(0,-1.2) -| (tier1.north);

% Context labels
\node[draw=none, font=\footnotesize, text=gray] at (3,1.2) {Latencia típica:};
\node[draw=none, font=\footnotesize, text=gray] at (3,0.7) {Tier 0: < 1 ms (RAM) / 1-4 s (HW)};
\node[draw=none, font=\footnotesize, text=gray] at (3,0.2) {Tier 1: 300-500 ms};
\node[draw=none, font=\footnotesize, text=gray] at (3,-0.3) {Tier 2: 5-30 s};

\end{tikzpicture}
\end{document}
